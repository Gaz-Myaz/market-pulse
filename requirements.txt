streamlit>=1.32.0
yfinance>=0.2.36
# pandas-ta is intentionally omitted: its only PyPI releases now (0.4.x) require
# numpy>=2.2.6, which conflicts with the pinned pandas==2.1.4 (numpy<2). Technical
# indicators (RSI, MACD, Bollinger %B, SMA) are computed manually in src/data.py.
# If you install a numpy<2-compatible pandas-ta, src/data.py auto-detects and uses it.
scikit-learn>=1.4.0
xgboost>=2.0.0
# FinBERT runs via the HuggingFace Inference API (see src/sentiment.py), not a
# local model — so torch/transformers are intentionally NOT dependencies. This
# keeps the image small enough for Streamlit Cloud's ~1GB free-tier RAM.
feedparser>=6.0.11
supabase>=2.3.0
plotly>=5.19.0
pandas==2.1.4
numpy>=1.26.0
python-dateutil>=2.8.2
pandas-market-calendars>=4.3.0
requests>=2.31.0
